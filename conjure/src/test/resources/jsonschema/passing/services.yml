services:
  TestService:
    name: Test Service
    package: test.api
    endpoints:
      get:
        http: GET /get/{simpleId}
        args:
          simpleId:
            type: SimpleId
        returns: SimpleObject
