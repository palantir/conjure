types:
  definitions:
    objects:
      MyEnum:
        values:
          - ONE
          - TWO
      MyObject:
        fields:
          hello: string
          maybe: optional<string>
          enum: MyEnum
      MyUnion:
        union:
          enum: MyEnum
          object: MyObject
