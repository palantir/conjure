types:
  imports:
    ResourceIdentifier:
      base-type: string
      external:
        java: com.palantir.ri.ResourceIdentifier

  definitions:
    default-package: test.api
    objects:
      InvalidJson:
        union:
          # SCHEMA(type, enum): not a valid FieldDefinition or ConjureType
          optionA: { }
