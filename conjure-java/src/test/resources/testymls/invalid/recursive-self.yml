types:
  definitions:
    default-package: test.api
    objects:
      # invalid: cannot have a concrete recursive reference
      SelfRecursiveObject:
          fields:
            self: SelfRecursiveObject
