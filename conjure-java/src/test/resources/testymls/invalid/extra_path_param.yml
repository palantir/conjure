services:
  TestService:
    name: Test Service
    package: test.a
    endpoints:
      testEndpoint:
        http:
          method: GET
          path: /path
        args:
          # invalid: cannot have a path param that does not occur in the path template
          arg:
            param-id: arg
            param-type: path
            type: string
        returns: string
