types:
  definitions:
    default-package: test.a.api
    objects:     
      SimpleObject:
        fields:
          stringField: string

services:
  TestServiceA:
    name: Test Service A
    package: test.a.api

    endpoints:
      get:
        http: GET /get
