types:
  imports:
    AuthHeader:
      base-type: string
      external:
        java: com.palantir.tokens.auth.AuthHeader

  definitions:
    default-package: test.a.api
    objects:     
      SimpleObject:
        fields:
          stringField: string

services:
  TestServiceA:
    name: Test Service A
    package: test.a.api
    default-auth: header

    endpoints:
      get:
        http: GET /get
