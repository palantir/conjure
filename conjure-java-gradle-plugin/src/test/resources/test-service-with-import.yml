types:
  conjure-imports:
    externalImport: external-imports/external-import.yml
    internalImport: internal-import.yml
  definitions:
    default-package: test.a.api
    objects:

services:
  TestServiceA:
    name: Test Service A
    package: test.a.api.with.imports

    endpoints:
      get:
        http: GET /get
        args:
          object: externalImport.SimpleObject
        returns: internalImport.SimpleObject

