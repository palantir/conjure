types:
  definitions:
    default-package: test.b.api
    objects:
      SimpleObject:
        fields:
          stringField: string

services:
  TestServiceB:
    name: Test Service B
    package: test.b.api

    endpoints:
      get:
        http: GET /get

