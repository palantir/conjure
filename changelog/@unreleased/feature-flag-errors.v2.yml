type: manualTask
manualTask:
  description: >
    Error types are disabled by default.
  actions:
  - Add `conjure { experimentalFeature 'ErrorTypes' }` to your build.gradle files if you want to use these.
